{
  "name": "eth_getTransactionCount",
  "summary": "Returns the number of transactions sent from an address",
  "params": [
    {
      "name": "transactionAddress",
      "summary": "address to query for sent transactions",
      "required": true,
      "schema": {
        "$ref": "../../components/address.json#/address"
      }
    },
    {
      "required": true,
      "name": "block number",
      "summary": "The number of blocks in the best chain of the trie",
      "schema": {
        "$ref": "../../components/Block.json#/number"
      }
    }
  ],
  "result": {
    "name": "transaction count",
    "schema": {
      "$ref": "../../components/Transaction.json#/count"
    }
  }
}
