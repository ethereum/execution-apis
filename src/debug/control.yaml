- name: debug_setHead
  summary: Rewinds the head of the chain to a previous block.
  params:
    - name: Number
      required: true
      schema:
        $ref: '#/components/schemas/uint64'
  result:
    name: None
    schema:
      type: "null"
