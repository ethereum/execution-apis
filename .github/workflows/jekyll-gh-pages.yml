name: "DISABLED: Jekyll Pages (replaced)"

# This workflow was disabled and replaced with a no-op in favor of the Docusaurus
# deploy pipeline (deploy.yaml). Kept as a manual dispatch in case someone
# needs to run it for debugging, but it no longer runs on push.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow has been disabled in favor of deploy.yaml (Docusaurus)."
